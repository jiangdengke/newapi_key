{
  "name": "newapi-key-importer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Import Anthropic keys into New API channels with sequential names.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
